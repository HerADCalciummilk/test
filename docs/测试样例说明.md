"""L1/L2 测试样例说明（本分支专用，勿合入 main 长期保留也可按需删）。

## 设计原则

本 PR **故意不放 L1 blocker 样例**，以便 job `l1` 通过后跑到 **L2**，一次看全流水线。
L1 blocker 请用文末「本地命令」单独演示。

## 本分支包含

| 路径 | 预期 L1 | 预期 L2 |
|------|---------|---------|
| `00temp/demo_algo_clean` | 通过；可能有 warning（`resource/` 仅占位、硬编码路径、src I/O） | 会审；**cli 仅占位、未调 process** → 宜写入 findings |
| `NIMM/02diagnostic/demo_official_ok` + 根级配套六树 | 通过 | 会审；cli 已调用 `process`，风险宜偏低 |
| `NIMM/utils/risky_util.py` | 非算法包；warning：`DANGEROUS_API`（`eval`）等 | 按变更文件语义审 |

## 开 PR 后看什么

1. Checks：**L1 machine review** 绿灯（仅有 warning）
2. PR 评论：L1 警告列表 + **L2 LLM** 对 clean 的 cli / utils 的评述
3. Artifact：`l1-review.json` / `l2-review.json`

## 本地演示 L1 blocker（不在本 PR 中）

可临时建缺目录包后：

```bash
python .github/scripts/review/l1_review.py --path 00temp/demo_algo_clean --json l1-clean.json
python .github/scripts/review/l1_review.py --path NIMM/02diagnostic/demo_official_ok --json l1-off.json
python .github/scripts/review/l2_review.py --path 00temp/demo_algo_clean --dry-run --json l2-dry.json
```
