"""L1/L2 测试样例说明（本分支用于模拟：发现问题 → 修复 → 再推送）。

## 本分支包含（修复后预期）

| 路径 | 预期 L1 | 预期 L2 |
|------|---------|---------|
| `00temp/demo_algo_clean` | 通过（无上述 warning） | cli 已调用 `process`，风险宜偏低 |
| `NIMM/02diagnostic/demo_official_ok` + 六树配套 | 通过 | cli 已调用 `process` |
| `NIMM/utils/risky_util.py` | 非包；无 `DANGEROUS_API`（已改 `ast.literal_eval`） | 按变更文件语义审，宜无明显高风险 |

## 流程演示

1. 第一轮：故意埋问题 → L1 warning + L2 findings  
2. 第二轮（本提交）：按评论修复并 push → 预期 Checks 仍绿、发现明显减少或消失  
"""
